{
  "name": "conveyor",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.7.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev": "docker compose up --build"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
